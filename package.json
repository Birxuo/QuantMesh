{
  "name": "quantmesh-root",
  "private": true,
  "scripts": {
    "dev": "cd dashboard && npm run dev",
    "install:all": "cd dashboard && npm install && cd .. && pip install -r provider/requirements.txt -r consumer/requirements.txt"
  },
  "dependencies": {
    "markdown-it": "^14.1.1",
    "next": "^16.2.4"
  }
}
